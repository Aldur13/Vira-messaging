[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "node node_modules/.bin/tsx src/index.ts"
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/health"
healthcheckTimeout = 30
